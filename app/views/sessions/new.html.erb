<h1>Sign in</h1>
<%= form_for :session, url: sign_in_path do |f| %>
	<p>
	<%= f.label :email %>
	<%= f.text_field :email %>
</p><p>
	<%= f.label :password %>
	<%= f.password_field :password %>
</p>
	<%= f.submit "Sign in" %>
<% end %>
<p> New User? <%= link_to "Sign un now!", sign_up_path %></p>
